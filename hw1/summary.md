## 🔎 Выводы о проделанной работе

### 📊 Часть 1: EDA

1. Появилось представление о содержании датасета: типы колонок, значения и тд., об основной задаче - прогнозирование цены авто и потенциальных признаках, которые можно использовать при прогнозаировании в качестве независмых переменных
2. Поработала со структурой данных:
    - выявила полные дубли и удалила их, так как потенциально они могут негативно сказаться на метриках, особенно если объект "аномальный" по характеристикам
    - выявила пропуски и заполнила их для числовых колонок медианой, так как эта мера центарльной тенденци более устойчива к выбросам
    - провела предварительную предобработку данных: привела колонки к правильному по логике типу данных, избавившись от единиц измерения и/или лишних символов 
3. Построила визуализации для лучшего понимания данных:
    - попарные распределения и графики корреляций помогли выдвинуть гипотезы о взаимосвязи признаков с целевой переменной, друг с другом -> о возможной мультиколлинеарности, например, max_power имела положительный высокий коэффициент корреляции с torque
    - одномерные гистограммы и ящик с усами помогли увидеть скошенное распределение у ряда числовых признаков -> далее в части feature engineering попробовала применить к колонкам логафм-трансформацию
    - а по некоторым визуализациям была заметна нелинейная связь с зависимой перменной, например, year
4. Ручной расчет корреляции Пирсона помог понять тонкости, которые неочевидны при использовании библиотек, например, что в случае малого числа уникальных значений нужна поправка
5. Выяснила, что в train выборки есть аномально высокие значения в некоторых признаках -> можем переобучится на них. Но в целом выборки train и test сопоставимы

### 📈 Часть 2: Модель только на вещественных признаках

1. Разбила train выборку на обучающую и валидационную выборки, чтобы по test выборке замерить итоговое качество лучшей модели
2. Классическая линейная регрессия дала невысокий r2 и достаточно высокую mse, что на train, что на валидационной, кажется, что модель слишком простая:
    - R2 на трейне: 0.6082 и R2 на валидационной: 0.5568
    - MSE на трейне: 104940597679 и MSE на валидационной: 160257133029
3. После стандратизации признаков качество модели не  улучшилось, зато она позволила посмотреть на feature importance, так как привели переменные к одинаковому масштабу. Самым значимым признаком для предсказания цены оказалась максимальная мощность двигателя (max_power), но ее коэффициент очень большой. Как выяснили ранее этот признак скоррелирован с torque -> сигнал о возможной мультиколинеарности -> надо попробовать добавить регуляризацию
4. L1-регуляризация не занулила веса коэффициентов и не улучшила метрики. Причина в том, что у нас изначально были высокие значения коэффициентов. Даже при переборе гиперпараметров модели параметр альфа, который отвечает за силу регуляризации, "скакал" от 1 до 300 при этом метрики особо не менялись -> возможно выбрала неудачный диапазон (слишком маленький или большой) или это и так оптимум при таком наборе признаков. Аналогично для ElasticNet, хотя доля l1 регуляризации варьировалась от 0 до 1
5. L0-регуляризацию не получилось реализовать в правильном виде, только примитивное приближение с помощью подсказки от chatgpt про зануление весов по определенному порогу

### ✨ Часть 3: С категориальными фичами

1. Добавление категориальных фичей улучшило метрики:
    - R2 вырос до 0.6842 с 0.5568 на валидационной
    - MSE уменьшилась в 1.4 раза
2. Использование l2-регуляриции вряд ли дало сильный эффект на рост метрик, так как применяли ранее в ElasticNet
3. Но на тестовой выборке получились ужасные метрики, очень похоже, что модель переобучилась, возможно, как раз из-за выбросов, про которые я в итоге забыла и не попыталась почистить:
    - R2 на df_test: 0.332
    - MSE на df_test: 383935797695

### 🎁 Часть 4 (бонус): Feature Engineering

1. Эту часть делала последней, поэтому творческая сторона к этому моменту устала и я пошла более простым путем:
    - применила для ряда числовых колонок со склошенным распределением логарифм-трансформацию (то, что заметила на eda)
    - переменную год возвела в квадрат (по eda)
2. Что можно было сделать еще:
    - почистить от выбросов
    - объединить малочастотные категории в более крупные категории

### 💼 Часть 4: Бизнесовая

1. Лучшей моделью по бизнесовой метрике - доля прогнозов, отличающихся от реальных цен на эти авто не более чем на 10%, оказалась ridge с вещественными и категориальными фичами. Значение метрики на тесте: 0.287, но это не сильно выше, чем у линейной регрессии без регуляризации и категориальных фичей = 0.229 -> не хватает предсказательной силы для точного прогноза -> нужны новые признаки или нелинейная модель или и то и другое
2. Для моей метрики - не симметричной: при недопрогнозе считаем квадрат отклонения, а при перепрогнозе - модуль, тем самым сильнее штрафуем за недопрогноз, снова лучшей оказалась ridge с вещественными и категориальными фичами

### 📱Часть 5: Создание интерактивного приложения на Streamlit

1. На мой взгляд, приложение достаточно удобное и простое:
    - есть загрузка csv
    - есть пример строк датафрейма
    - выведены описательные статистики по колонкам
    - отображены графики о взаимосвязи признаков друг с другом и зависимой переменной
    - все это помогает получить представление о данных
2. Далее выведены веса модели в формате feature importance с цветом, который помогает понять направление влияния признака
3. И наконец можно сделать предсказание цены авто как по заданным параметрам, так и по загруженным в формате csv данным
4. Проблемы:
    - нет четкой валидации данных на пропуски и нетипичные значения
    - можно загружать только csv
    - модель не дообучается на новых данных, всегда используем одни и те же признаки и модель
5. Улучшения:
    - интеративные графики для совместных распределений - с перетаскиванием колонок в форму для осей X и Y
    - одномерные распределения для каждого признака, сейчас не хотелось перегружать
    - выводить прогнозный интервал, полезно для понимания точности прогноза